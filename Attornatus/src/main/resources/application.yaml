spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:avaliacao
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
  h2:
    console:
      enabled: true
      path: /h2-console